<%= form_for([@post, @comment]) do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :text %>
  <%= f.text_field :text %>

  <%= f.label :user %>
  <%= f.text_field :user %>

  <%= f.submit %>

<% end %>
