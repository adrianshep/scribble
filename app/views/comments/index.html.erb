<h1>Scribble</h1>
<h2>Comments <% link_to "(new)", new_post_comment_path %></h2>

<ul>
  <% @comments.each do |comment| %>
    <li>
      <%= link_to "#{comment.title} - #{comment.post.title}", post_comment_path(comment.post, comment) %>
    </li>
  <% end %>
</ul>
